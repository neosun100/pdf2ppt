# Environment variables for PDF2PPT

# Server Configuration
PORT=8000

# Conversion Settings
DEFAULT_PARALLEL_WORKERS=4
MAX_FILE_SIZE_MB=100

# Paths (inside container)
UPLOAD_DIR=/tmp/pdf2ppt/uploads
OUTPUT_DIR=/tmp/pdf2ppt/outputs
